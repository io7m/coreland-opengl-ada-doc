(subsection
  (title "External Package Requirements")
  (table package_deps
    (t-row (item "Package")                         (item "Version"))
    (t-row (item (link-ext "http://lua.org" "Lua")) (item version ">= 5.1.4")))

  (para (link-ext "http://lua.org" "Lua") " is required to generate part of
    the code. It is not a run-time requirement - the resulting library/code
    does not depend on " (link-ext "http://lua.org" "Lua") " in any way, shape
    or form."))
