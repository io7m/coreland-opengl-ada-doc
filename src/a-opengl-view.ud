(section
  (title "OpenGL.View")
  (ref "opengl_view")
  (contents)

  (para "The " (item ada_package "OpenGL.View") " package provides
    access to viewport handling.")

  (include "a-opengl-view-depth_range.ud")
  (include "a-opengl-view-viewport.ud")
)
