(section
  (title "OpenGL.View")
  (ref "opengl_view")

  (para "The " (item ada_package "OpenGL.View") " package provides
    access to viewport, transformation and matrix handling.")

  (include "a-opengl-view-depth_range.ud")
)
